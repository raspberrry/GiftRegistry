Gift Registry

Requires Xampp
	--MySQL
	--Apache

Import the .sql files given in the Sample Database folder under a database called 'wt'

Homepage_n.php	--Intro Page

Try.php		--Implementation for Login and Sign Up
--check.php		--Store new user sign up
--store.php		--Validation for login


Dash.php	--Dashboard of user 
--explore.php		--Adding new items to each list

--listall.php		--Viewing each user's lists and functionality for creating new list
  --createlist.php		--Displays the one of the maximum 3 lists the user has created

--Profile.php		--Options to edit Name, Date of Birth, City and Profile picture 
				(Profile picture can be chosen only from the images provided for now)

--calendar.php		--To display all the dates of the events on a calendar
				(Displays only one date currently)